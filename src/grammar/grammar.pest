Operation = _{ PInc | PDec, | VInc | VDec, Output, Input }
	PInc = {">" }
	PDec = { "<" }
	VInc = { "+" }
	VDec = { "-" }
	Output = { "." }
	Input = { "," }
Repetition = { "[" ~ expr ~ "]" }

Term = _{ operation | repetition }
Expr = { (WHITESPACE* ~ term ~ WHITESPACE*)* }

RsBrainfxxk = { SOI ~ expr ~ EOI }
WHITESPACE = _{ " " | "\t" | "\n" }
